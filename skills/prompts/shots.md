---
title: 分镜脚本规范
module: shots
rev: 10
note: 本文件是「分镜脚本」模块的风格规范，不是直接发给大模型的指令，而是与固定的任务说明、JSON 输出契约组装后再发送。本规范同时约束镜头、角色档案、场景档案三部分的写法。保存后下次生成分镜立即生效。注意：请保留 ## 小节标题，程序按标题读取内容。
---

## 适用范围

本规范约束「改编稿 → 镜头列表 + 角色档案 + 场景档案」这一环节。一次生成同时产出三份内容，三者必须互相一致：镜头里出现的角色、场景都要能在对应档案里逐字找到。

## 核心规则

- 一个连续动作单元 = 一个镜头。动作、空间或时间发生变化就切新镜头，不要把多件事塞进同一个镜头。
- 镜头语言写具体手法：全景 / 中景缓推 / 近景跟拍 / 特写 / 俯拍 / 过肩 / 手持晃动等。
- 对白保留改编稿原句，不新增台词；本镜头无对白就留空串。
- **对白必须带说话人名（硬要求）**：格式写成 `说话人名：台词原文`，冒号用全角「：」，**冒号前必须是说话人的名字**，且与角色档案里的名字逐字一致（多人说话就同一字段内换行，一行一句）。台词是画外音 / 独白 / 混响时，在人名后用括号标注，如 `李白（独白/混响）：故人西辞黄鹤楼。`
  🔴 为什么必须带名字：下游生成 H3 视频提示词时，程序按冒号前的人名把每句台词**钉死**给对应角色；名字缺失或写成「（旁白）」「少女的声音」这类描述，台词就会被模型随便分给画面里的人（曾有镜头该孟浩然说话、成片却由李白开口）。人名只能是角色档案里存在的角色名。
- 镜头按时间顺序排列，覆盖改编稿的全部内容，不跳段、不重复。
- 粒度参考：每 1000 字改编稿约 8-15 个镜头，符合短剧快节奏。

## 时长规则（重要）

时长由镜头内容决定，宁短勿长，最终取 4-15 之间的整数秒（含 4 与 15）：

- 有对白：按 对白字数 ÷ 4.5 字/秒 + 起止停顿约 1 秒 估算（例：12 字对白 ≈ 3.7 秒 → 取 4 秒）。**同一镜头里还有台词之外的大段发声表演（大笑、痛哭、惊呼、喘息等）时，每段再额外加 1.5-2 秒；估算结果一律向上取整**（例：25 字台词 + 一段大笑 ≈ 5.6 + 2 → 取 8 秒，而不是砍成 6 秒）。台词加表演实在塞不下时，拆成两个镜头或精简表演，**绝不让声音事件被挤到镜头时长之外**——时长不够的视频里，笑声、哭声都会被直接丢掉。
- 无对白、单一动作：4-5 秒。
- 无对白但动作有起承转合（走位、转身、情绪变化）：6-8 秒。
- 多句对白或复杂调度：8-12 秒；只有镜头内包含完整事件链时才给 12-15 秒。
- 禁止为了"显得完整"给所有镜头统一写 6 秒或 8 秒；同一场景内的多个短镜头应各自取 4-6 秒。

## 镜头衔接（重要）

相邻镜头必须像剪辑过的连续片段，逐对接力检查（1-2、2-3……每一对都过）：

- **动作衔接**：上一镜头结束时的状态（人物位置、姿势、手中道具）是下一镜头的起点，不得凭空跳变。例：上一镜酒壶已经掏出，下一镜不能又从掏壶开始；上一镜人在坟前，下一镜不能无故换了站位。
- **对话衔接**：对话按真实顺序排列，问句的下一镜就是答句；同一人连续说话合并进同一镜头，不拆散。
- **景别衔接**：相邻镜头的收尾景别避免重复；同一场景内景别宜递进（远→中→近），情绪爆点才给特写，特写不宜连续超过两个。
- **文案连读**：把所有镜头的 action 按顺序连读，必须像一段通顺叙事——不得出现指代断裂（「他」不知指谁）、信息倒挂（道具用到后才在后一镜交代来历）。
- **方向连贯**：同一场景内人物的相对位置与运动方向保持一致，不无故跳轴。

**输出前自检**：逐对检查相邻镜头以上五条，任何一对不达标，先修再输出。

## 格式与一致性

- 场景命名统一为「地点·内外-时段」（例：教室·内-日）。
- 同一场景在多个镜头中出现，每个镜头都必须写完整场景名，严禁出现"同上""同上场景""相同""一致"这类省略写法。
- 镜头里的场景名必须与场景档案中的名字逐字一致；角色名必须与角色档案中的名字逐字一致。
- **分镜与档案同批产出，且档案先于提示词**：角色 / 场景档案（profile）是「谁、什么身份、长什么样」的事实记录，提示词只是把档案翻译成摄影描述。档案写法（穷举点名、从台词反推身份、差异化矩阵、字段格式）见「档案规范」（profile.md），本批档案必须遵守它。
- 角色档案（characters）：每个出场角色一条，字段 name（沿用原著名）、role（主人公 / 配角 / 反派等）、profile（中文档案：每行一个字段「字段名：值」，全角冒号，字段顺序 姓名 / 身份 / 性别 / 年龄段 / 身形体态 / 脸型骨架 / 发型发饰 / 胡须 / 服装 / 配饰道具 / 主色调 / 神态 / 依据，**不得留空、不得写「未知」**，每人都要有 1 个识别锚点，任意两个角色至少 3 个维度明显不同）、prompt（中文生图提示词：**摄影定调**（极其逼真的真人古装摄影作品，8K超高清，单反相机人像镜头拍摄）→ 年龄性别 → 脸型五官 → 真实皮肤纹理与毛孔、真实发丝 → 古风服装的款式/材质/颜色与真实布料褶皱光泽 → 神态 → 摄影术语（黄金时刻光线、浅景深背景虚化、电影级调色）→ **真人化收尾**（真人古装剧剧照，照片级真实），全部用逗号分隔的短语，不要整句、不要出现文字水印说明。提示词必须忠于该角色的 profile，不得写入档案里没有任何线索的关键设定）、negative（中文负向提示词：**必须含反动漫词表**（动漫、二次元、插画、绘画、3D、游戏CG、卡通、塑料感）+ 质量负面词 + 该角色需排除的元素，逗号分隔短语，不得与正向矛盾）。
- 场景档案（scenes）：每个出现过的场景一条，字段 name（与镜头中逐字一致）、profile（中文档案：每行一个字段「字段名：值」，字段顺序 场景名 / 内外 / 时段 / 空间结构 / 主要陈设 / 材质细节 / 光线与色温 / 天气氛围 / 空镜声明，其中「空镜声明」必须是正面陈述且不得出现「剧照 / 演员 / 人像」这类召人词）、prompt（中文生图提示词：**摄影定调**（极其逼真的古装剧实景照片，8K超高清，单反相机拍摄）→ **空镜声明**（古装剧实景布景空镜，画面里空无一人，只有建筑与环境陈设）→ 室内外 → 空间结构 → 主要陈设 → 真实材质细节 → 时间与光线（摄影术语）→ 天气 → **真人化收尾**（古装剧实景布景照片，照片级真实），同样用逗号分隔短语。🔴 场景图必须是空镜：正向提示词里**绝对不得**出现 `剧照`、`演员`、`人像`、`人物` 这类会把人召进画面的词（「剧照」尤其禁止），也**不得**用 `无人物`、`不要出现人物` 这类否定写法——空镜只能用「古装剧实景布景空镜，画面里空无一人，只有建筑与环境陈设」这种**正面陈述**表达。同样可在忠于改编稿的前提下补充空间层次、光线方向与色温、材质质感等合理细节，不得与原文冲突）、negative（中文负向提示词：反动漫词表 + 质量负面词 + 该场景需排除的元素，不得与正向矛盾）。
- 全片画面风格统一为**古风真人实拍**（真人古装剧质感）：角色与场景的定调词核心与收尾核心必须一致（真人古装、8K超高清、单反相机拍摄、照片级真实），全片不得出现动漫 / 插画 / 3D 渲染等风格词；场景的收尾**不得**用「剧照」二字。

## 兜底默认

（无）
